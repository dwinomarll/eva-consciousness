# Copy to .env and fill in your keys
ANTHROPIC_API_KEY=
NOTION_TOKEN=
OMI_API_KEY=

# Tether: set a token to require `Authorization: Bearer <token>` on POST /chat.
# Leave blank to run open (local only).
EVA_AUTH_TOKEN=

# Override the shared memory root (defaults to ~/eva-workspace).
# EVA_WORKSPACE=

# Ma'at MCP: optional local roots used by maat-mcp.
# MAAT_WORKSPACE=~/eva-workspace
# MAAT_REPO_PATH=~/maat-repo

# ── Slack MCP ─────────────────────────────────────────────────────
SLACK_BOT_TOKEN=
SLACK_TEAM_ID=

# ── Google Calendar MCP ───────────────────────────────────────────
GOOGLE_OAUTH_CREDENTIALS=

# ── Gmail MCP ─────────────────────────────────────────────────────
# One-time auth: npx @gongrzhe/server-gmail-autoauth-mcp auth
# GMAIL_OAUTH_PATH=
